[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "nexus-bus-gui"
version = "0.1.0"
description = "nexus-bus-gui - PySide6 GUI"
requires-python = ">=3.9"
dependencies = ["PySide6>=6.5", "grpcio>=1.50", "protobuf>=3.21"]
